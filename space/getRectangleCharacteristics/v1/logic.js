let height = prompt("Dame el lado del rectangulo: ");
let width = prompt("Dame el lado del rectangulo: ");
console.log(
  "Perímetro: " + (2 * (height + width)) + "\n"
  + "Area: " + (height * width));
  